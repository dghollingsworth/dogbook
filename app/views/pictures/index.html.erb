<h1>Pictures</h1>
<% @pictures.each do |picture| %>
	<div class="picture">
		<p>
			<%= picture.description %><br/>
			<%= link_to picture.url %><br/>
			<%= link_to "Show", picture_path(picture.id) %>
		</p>
	</div>
<% end %>

